name: Expressions Lab

on:
    workflow_dispatch:
    pull_request:

jobs:
    logic-test:
        runs-on: ubuntu-latest
        steps:
            - name: greet boss 
              run: |
                echo "${{github.ref_name}}"
                echo "${{github.actor}}"
                echo "${{github.event_name}}"
                echo "${{github.repositoryUrl}}"
